import React from "react";


function Hooks0009(props){
    return (
        <div className="user10">
            <span>{props.data.name}</span>
            <span>{props.data.email}</span>
            <span>{props.data.contact}</span>
        </div>
    )
}

export default Hooks0009;